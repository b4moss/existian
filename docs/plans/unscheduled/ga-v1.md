# v1.0.0 GA

- **状態**: 意図スタブ
- **マイルストーン**: unscheduled（版は PO 判断で `v1.0.0` に割当）

## 目的

公開 API・属性セット・状態モデルが安定し、SemVer 1.x として後方互換を約束できる状態にする。

## ざっくり範囲

- API / 属性の凍結方針
- `docs/specs/` と利用ドキュメントの整合
- 既知の破壊的変更候補の解消または明示的延期
- versioning-rule に従い main 上でタグ

## やらぬこと

- GA 前にスコープを膨らませる新機能の大量投入

----

以上
