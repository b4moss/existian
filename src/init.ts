import type { InitOptions } from "./types.js";

/**
 * Bind declarative async checks under `root`.
 * Stub for package bootstrap; implemented in later slices.
 */
export function init(_options?: InitOptions): void {}
